from setuptools import setup, find_packages

setup(
    name="nls",
    version="1.0.0",
    description="NLS (Nexus Ladder Scheduler) - Python client for Nexus credential store and script management",
    packages=find_packages(),
    python_requires=">=3.8",
)
